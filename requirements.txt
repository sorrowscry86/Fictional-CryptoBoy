# Trading Framework
freqtrade>=2023.12  # Core trading bot framework with exchange integration, strategy execution, and backtesting

# Data Processing
pandas>=1.5.0   # OHLCV data manipulation, sentiment merging, time-series analysis
numpy>=1.24.0   # Numerical operations for technical indicators and price calculations

# API Clients
requests>=2.28.0        # HTTP client for REST API calls (news aggregation, LM Studio)
ccxt>=4.1.0             # Unified exchange API (market data collection, order execution)
python-binance>=1.0.0   # Binance-specific API client with advanced features

# Microservices Infrastructure
redis>=4.5.0    # Sentiment cache client (reads sentiment:{pair} hashes for trading decisions)
pika>=1.3.0     # RabbitMQ client library (message queue communication between services)
docker>=6.0.0   # Docker SDK for Python (used in test infrastructure and health checks)

# News & RSS
feedparser>=6.0.0      # RSS feed parsing (CoinDesk, CoinTelegraph, Decrypt, Bitcoin Magazine)
beautifulsoup4>=4.11.0 # HTML parsing for article content extraction and cleaning
lxml>=4.9.0            # High-performance XML/HTML parser (feedparser backend)

# Technical Analysis
ta>=0.10.0      # Technical indicators library (EMA, MACD, Bollinger Bands, Volume)
ta-lib>=0.4.0   # C-based TA library for faster indicator calculation (RSI, MACD)

# Machine Learning & LLM
transformers>=4.30.0  # Hugging Face transformers (FinBERT sentiment analysis - primary model)
torch>=2.0.0          # PyTorch backend for FinBERT inference (35s load, real-time processing)
scikit-learn>=1.0.0   # ML utilities (feature scaling, data preprocessing for backtesting)

# Telegram Bot
python-telegram-bot>=20.0  # Telegram notifications (trade alerts, errors, performance reports)

# Utilities
python-dotenv>=1.0.0  # Environment variable management (.env file loading for API keys)
pyyaml>=6.0           # YAML parsing (Freqtrade config files, strategy parameters)
colorlog>=6.7.0       # Colored logging output (improves log readability in development)

# Testing
pytest>=7.4.0       # Test framework (unit, integration, e2e tests in tests/ directory)
pytest-cov>=4.1.0   # Coverage reporting (configured in .coveragerc, excludes tests/data/logs)

# LLM Client
httpx>=0.24.0    # Modern async HTTP client (LM Studio API calls with connection pooling)
aiohttp>=3.9.0   # Async HTTP server/client (monitoring dashboard WebSocket, API endpoints)
