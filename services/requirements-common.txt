# Common dependencies for CryptoBoy microservices
# Shared by: news_poller, sentiment_processor, signal_cacher
# Note: Excludes ccxt.pro websockets (only in requirements-ingestor.txt)

# Message Queue
pika>=1.3.0  # RabbitMQ client for inter-service communication (raw_news_data, sentiment_signals_queue)

# Cache
redis>=4.5.0  # Redis client for sentiment cache writes (signal_cacher) and reads (trading bot)

# Data Processing
pandas>=1.5.0  # CSV data handling for legacy mode, data validation
numpy>=1.24.0  # Numerical operations for sentiment score calculations

# HTTP Clients
requests>=2.28.0  # RSS feed fetching, LM Studio API calls, HTTP-based integrations

# News Aggregation
feedparser>=6.0.0      # RSS feed parsing from 5 crypto news sources (CoinDesk, CoinTelegraph, etc.)
beautifulsoup4>=4.11.0 # HTML content extraction and cleaning for article bodies
lxml>=4.9.0            # High-performance XML/HTML parser (feedparser dependency)

# Configuration
python-dotenv>=1.0.0  # Environment variable loading (.env) for service-specific configs

# Logging
colorlog>=6.7.0  # Colored structured logging for service health monitoring and debugging

# LLM / Sentiment Analysis
transformers>=4.30.0  # FinBERT (ProsusAI/finbert) - primary sentiment analysis model
torch>=2.0.0          # PyTorch backend for FinBERT inference (sentiment_processor service)
