# Data Ingestor Service Dependencies
# Used by: market_streamer (real-time WebSocket OHLCV data)
# Separate from requirements-common.txt to isolate ccxt.pro websocket dependencies

# Message Queue
pika>=1.3.0  # RabbitMQ client for publishing market data to raw_market_data queue

# Cache
redis>=4.5.0  # Redis client for caching recent market data (optional optimization)

# Data Processing
pandas>=1.5.0  # OHLCV data frame construction, time-series handling
numpy>=1.24.0  # Price and volume calculations, statistical operations

# HTTP Clients
requests>=2.28.0  # REST API fallback for exchange connectivity checks

# Exchange Integration
ccxt>=4.1.0  # Unified exchange API with WebSocket support (Binance, Coinbase, Kraken)
             # IMPORTANT: ccxt.pro provides async WebSocket streaming for real-time market data
             # Isolated here to prevent conflicts with other services using REST-only CCXT

# Configuration
python-dotenv>=1.0.0  # Environment variable loading (TRADING_PAIRS, CANDLE_TIMEFRAME, etc.)

# Logging
colorlog>=6.7.0  # Structured logging for WebSocket connection health and data flow monitoring
